-
  event_dates:
    start: "2022-09-01"
    end: "2022-09-15"
  text_dates:
    # Adjust these if the text + callout shape should appear for
    #   different dates than the event appears on the date wheel
    start: "2022-09-01"
    end: "2022-09-15"
  # Defaults to 9
  fade_in:
  # Defaults to 9
  fade_out:
  text:
    label: ["Deepening drought in", "the Great Plains"]
    # This is in percentage space, relative to the plotting device
    x_loc: 0.59
    y_loc: 0.69
    # Numeric indicating where to place text relative to location: 1,2,3,4 (below, left, above, right)
    pos: 1
    cex: 6
  # TRUE or FALSE, defaults to FALSE
  add_box:
  # "#04507d" for wet event & "#ca0020" for a dry event, defaults to wet
  wheel_color: "#ca0020"
  # Integer, starting with 0 (use >= 1 to increase height)
  wheel_hierarchy: 1
  polygon:
    # Defaults to existing small NE shape
    file: "6_visualize/in/overlay_callouts_dummy.png"
-
  event_dates:
    start: "2022-09-09"
    end: "2022-09-12"
  text_dates:
    # Adjust these if the text + callout shape should appear for
    #   different dates than the event appears on the date wheel
    start: "2022-09-09"
    end: "2022-09-12"
  # Defaults to 9
  fade_in:
  # Defaults to 9
  fade_out:
  text:
    label: ["Intense rainfall from", "Tropical Storm Kay caused", "flash floods and mudslides","in Southern California"]
    # This is in percentage space, relative to the plotting device
    x_loc: 0.35
    y_loc: 0.57
    # Numeric indicating where to place text relative to location: 1,2,3,4 (below, left, above, right)
    pos: 2
    cex: 6
  # TRUE or FALSE, defaults to FALSE
  add_box:
  # "#04507d" for wet event & "#ca0020" for a dry event, defaults to wet
  wheel_color: "#04507d"
  # Integer, starting with 0 (use >= 1 to increase height)
  wheel_hierarchy: 1
  polygon:
    # Defaults to existing small NE shape
    file: "6_visualize/in/overlay_callouts_dummy.png"
-
  event_dates:
    start: "2022-09-05"
    end: "2022-09-06"
  text_dates:
    # Adjust these if the text + callout shape should appear for
    #   different dates than the event appears on the date wheel
    start: "2022-09-05"
    end: "2022-09-06"
  # Defaults to 9
  fade_in:
  # Defaults to 9
  fade_out:
  text:
    label: ["Extreme rainfall event", "brought record-breaking", "precip and flash floods","to Northwest Georgia"]
    # This is in percentage space, relative to the plotting device
    x_loc: 0.66
    y_loc: 0.20
    # Numeric indicating where to place text relative to location: 1,2,3,4 (below, left, above, right)
    pos: 4
    cex: 6
  # TRUE or FALSE, defaults to FALSE
  add_box:
  # "#04507d" for wet event & "#ca0020" for a dry event, defaults to wet
  wheel_color: "#04507d"
  # Integer, starting with 0 (use >= 1 to increase height)
  wheel_hierarchy: 1
  polygon:
    # Defaults to existing small NE shape
    file: "6_visualize/in/overlay_callouts_dummy.png"
